{
  "name": "faker-data",
  "version": "0.0.1",
  "description": "Create fake random data using package `faker`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "#niteroomcreation",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0"
  }
}
